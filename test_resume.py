from resume_rating import rate_resume  # Import the function from your file

# Path to a sample resume PDF for testing
pdf_path = "sample_resume.pdf"

# Run the function and print the output
result = rate_resume(pdf_path)
print(result)
